#include<iostream>
using namespace std;

int main() {

    // example 1
    // int age = 19;
    // (age > 18) ? cout << "can vote!" : cout << "cannot vote!";
    // return 0;

    // example 2
    // int x = 5;
    // int y = 10;

    // int result = x>y ? x+y : x-y;
    // cout << "Result is: " << result << endl;
    // return 0;

    // example 3
    // int a = 10, b = 20;

    // a > b ? cout << "a is greater" : cout << "b is greater" << endl;
   
    // // another method
    // int max = a > b ? a : b;
    // cout << "Max no. is: " << max;
    // return 0;

    // example 4
    // int a = 10, b = 20;

    // string result = (a == b) ? "Equal" : (a > b) ? "A is greater" : "B is greater";
    // cout << "Comparison result: " << result << endl;
    
    // return 0;
}